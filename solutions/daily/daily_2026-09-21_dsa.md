# DevQuest Solution: 🔥 Today's DSA: Valid Anagram (2026-09-21)

- **Track**: 🔥 Daily Quests
- **Difficulty**: Beginner
- **Completed**: 2026-09-21 05:38:23 UTC
- **XP Earned**: +45 pts

---

## Problem Statement

**📅 Date:** `2026-09-21` | **🔥 Daily Bonus:** `+10 XP Included`

### 🧠 Daily DSA: Valid Anagram

Given two strings `s` and `t`, return `True` if `t` is an anagram of `s`, and `False` otherwise.

An Anagram is a word formed by rearranging the letters of a different word, using all the original letters exactly once.

**Example 1:** `s = "anagram"`, `t = "nagaram"` ➔ `True`
**Example 2:** `s = "rat"`, `t = "car"` ➔ `False`

---

## Verified Solution (python)

```python
def is_anagram(s: str, t: str) -> bool:
    if len(s) != len(t):
        return False

    counts = {}

    for c in s:
        counts[c] = counts.get(c, 0) + 1

    for c in t:
        if c not in counts or counts[c] == 0:
            return False

        counts[c] -= 1

    return True
```

## Verification Status
- All automated test cases passed successfully.
- Tested with DevQuest TUI runner.
